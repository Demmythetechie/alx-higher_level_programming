#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    ls = my_list
    if idx < 0:
        return my_list
    elif idx >= 0:
        return my_list
    else:
        ls[idx] = elements
        return ls
